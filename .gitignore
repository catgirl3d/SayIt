# ── Secrets ──
.env
.env.local
.env.*.local
config.yaml
_MIGRATION/
*.pem

# ── Development-only (not for open source) ──
dev-docs/
dev-scripts/
dev-notes/
we-media/
sayit说话波形图效果.html

# ── Server dev artifacts ──
server/.kiro/
server/.pytest_cache/
server/.ruff_cache/
server/docs/
server/scripts/
server/benchmarks/
server/mytestwav.txt

# ── Client dev artifacts ──
client/scripts/*
!client/scripts/check-i18n.mjs
client/dist/
# build.rs 从 transcribe-cpp 的构建产物拷进来的 DLL，纯生成物
client/src-tauri/transcribe-libs/

# ── Kiro IDE ──
.kiro/

# ── Node ──
node_modules/

# ── Rust build output ──
target/
# 应用在跑时它锁着 target/debug 里的 ggml DLL，cargo 会报 os error 32；
# 换个 target 目录编译是绕过它的标准做法（pitfalls #4），别让产物进仓库。
target-check/

# ── Python ──
__pycache__/
*.pyc
.venv*/

# ── OS ──
.DS_Store
Thumbs.db

# ── IDE ──
.vscode/
.idea/

# ── Tauri dev artifacts ──
*.blockmap

# ── Logs & runtime ──
*.log
runtime/
